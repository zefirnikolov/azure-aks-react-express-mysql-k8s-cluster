All the variables in webshop/ are put in locals.tf which is in .gitignore for cleaner code.

For proper deploy the webshop/ need a tls certificates path, so they it's deployed on the AKS and on the Azure AG
